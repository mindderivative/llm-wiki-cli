"""compiler — orchestrate summarize -> extract -> cascade-update -> embed.

`CompilerEngine` lives here. Cascade updates merge into existing notes
rather than only creating stubs for missing ones (ARCHITECTURE.md §7).
Not yet implemented.
"""
