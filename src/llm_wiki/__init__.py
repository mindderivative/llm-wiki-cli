"""llm_wiki — the pure-Python engine behind LLM-Wiki.

No GUI dependency lives in this package (ARCHITECTURE.md §2.5). Every
interface (CLI, MCP, GUI) is a thin consumer of the API exposed here.
"""

__version__ = "0.1.0"
