"""graph — build/maintain the NetworkX link graph incrementally.

`LinkEngine` lives here. Incremental via `content_hash` (`notes`/`links`
tables, §6); full rebuild only on explicit request. Not yet implemented.
"""
