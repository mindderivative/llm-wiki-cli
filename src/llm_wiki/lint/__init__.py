"""lint — schema validation, broken-link detection, health scoring.

`LintEngine` lives here. Reads from the DB cache instead of re-parsing
every file (ARCHITECTURE.md §8 `/wiki-lint`). Not yet implemented.
"""
