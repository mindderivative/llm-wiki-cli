"""llm — wrap llama-server's OpenAI-compatible endpoint.

`LlamaClient` (chat + structured extraction via `outlines`) and
`EmbeddingService` live here. No cloud provider SDKs enter this package
(ARCHITECTURE.md §2.3). Not yet implemented.
"""
